#include <iostream>using namespace std;struct hello {
    string getHello(string s) {
        return hello + s;
    }
}

double sqr(int x) {
    return x * x;
}

double sqrt(double x) {
    return x;
}

int main() {
    int i = 0;
    i++;
    for (int i = 0; i < n; i++) {
        cout << hellogetHello + "Vlad";
        for (int j = 0; j < n; j++) {
            ban = "BAN";
        }
        while (true) {
            baka = true;
            cout << "BAKAAAAA";
        }
    }
    c = (a + b);
    c = sqrt * (ax - bx);
    cout << "HI";
    return 0;
}