#include <iostream>

using namespace std;

struct hello
{
	string hello = "Hello, ";

	string getHello(string s)
	{
		return hello + s + "!";
	}
}

double sqr(int x)
{
	return x * x;
}

double sqrt(double x)
{
	return sqrt(x);
}

double empty(int ** );

int main()
{
	int i = 0;
	i++;
	for (int i = 0; i < n; i++)
	{
		cout << hello.getHello("Vlad");
		for (int j = 0; j < n; j++)
		{
			ban = "BAN!";
		}
		while (true)
		{
			baka = true;
			cout << "BAKAAAAA!!!";
		}
	}
	switch (i)
	{
		case 1:
			cout << 1 << endl;
			break;
		case 2:
			cout << 22 << endl;
			break;
		}
	c = (a + b) + (a * b) / 2;
	c = sqrt(sqr(ax - bx) + sqr(ay - by));
	cout << "HI";
	return 0;
}

