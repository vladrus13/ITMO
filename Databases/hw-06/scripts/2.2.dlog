WithMarks(StudentId) :-
  Students(StudentId, _, _),
  Marks(StudentId, CourseId, _),
  CourseId = :CourseId.
WithoutMarks(StudentId, StudentName, GroupName) :-
  Students(StudentId, StudentName, GroupId),
  Groups(GroupId, GroupName),
  ¬ WithMarks(StudentId).
