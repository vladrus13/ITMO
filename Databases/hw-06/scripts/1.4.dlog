NamesStudents(StudentId, StudentName, GroupId) :- Students(StudentId, StudentName, GroupId),
    Marks(StudentId, CourseId, Mark),
    Courses(CourseId, CourseName),
    Mark = :Mark,
    CourseName = :CourseName.
